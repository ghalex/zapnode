{
  "name": "zapnode",
  "version": "0.0.3",
  "main": "dist/zapnode.umd.js",
  "module": "dist/zapnode.es.js",
  "types": "dist/zapnode.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rm -rf dist && rollup -c",
    "types": "tsc -P tsconfig.dts.json",
    "prebuild": "rm -rf dist",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "esbuild": "^0.16.4",
    "rollup": "^3.7.2",
    "rollup-plugin-dts": "^5.0.0",
    "rollup-plugin-esbuild": "^5.0.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "rxjs": "^7.6.0"
  }
}
